apiVersion: v1
kind: Service
metadata:
  name: mcpserver
  namespace: mcpserver
  labels:
    app: mcpserver
spec:
  type: ClusterIP
  selector:
    app: mcpserver
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
